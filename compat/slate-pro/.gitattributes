# Normalize line endings — LF in the repo, native in the working copy.
* text=auto eol=lf

# Binary fixtures
*.png binary
*.jpg binary
*.jpeg binary
*.tar.gz binary

# Never normalize / never auto-handle key material (defense in depth;
# .gitignore already excludes these).
*.pem binary
